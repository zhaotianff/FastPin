<Window x:Class="FastPin.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FastPin"
        xmlns:vm="clr-namespace:FastPin.ViewModels"
        xmlns:models="clr-namespace:FastPin.Models"
        xmlns:conv="clr-namespace:FastPin.Converters"
        mc:Ignorable="d"
        Title="{conv:Localize AppTitle}" Height="600" Width="900"
        Background="{StaticResource BackgroundBrush}"
        WindowStartupLocation="CenterScreen"
        AllowsTransparency="False"
        ResizeMode="CanResize">
    
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="40"
                      ResizeBorderThickness="5"
                      GlassFrameThickness="1"
                      CornerRadius="0"
                      UseAeroCaptionButtons="False"/>
    </WindowChrome.WindowChrome>
    
    <Grid x:Name="MainGrid" Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource PrimaryBrush}" Padding="20,15">
            <Grid>
                <!-- Hamburger Menu Button (Left) -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                    <Button Content="☰" 
                            Click="DrawerMenuButton_Click"
                            Width="40" Height="30"
                            Foreground="White"
                            Background="Transparent"
                            BorderThickness="0"
                            Cursor="Hand"
                            FontSize="20"
                            VerticalContentAlignment="Center"
                            WindowChrome.IsHitTestVisibleInChrome="True"
                            ToolTip="Menu">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" 
                                                    BorderThickness="0"
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#106EBE"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
                
                <TextBlock Text="FastPin" FontSize="24" FontWeight="Bold" Foreground="White" VerticalAlignment="Center" Margin="50,0,0,0"/>
                
                <!-- Custom Window Control Buttons -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <!-- Settings Button -->
                    <Button Content="⚙" 
                            Click="SettingsButton_Click"
                            Width="40" Height="30"
                            Margin="0,0,5,0"
                            Foreground="White"
                            Background="Transparent"
                            BorderThickness="0"
                            Cursor="Hand"
                            FontSize="18"
                            VerticalContentAlignment="Center"
                            WindowChrome.IsHitTestVisibleInChrome="True"
                            ToolTip="Settings">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" 
                                                    BorderThickness="0"
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#106EBE"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                    
                    <!-- Minimize Button -->
                    <Button Click="MinimizeButton_Click"
                            Width="40" Height="30"
                            Margin="0,0,5,0"
                            Background="Transparent"
                            BorderThickness="0"
                            Cursor="Hand"
                            WindowChrome.IsHitTestVisibleInChrome="True">
                        <Button.Content>
                            <Path Data="M 0,0 L 10,0" 
                                  Stroke="White" 
                                  StrokeThickness="1"
                                  Stretch="None"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center"/>
                        </Button.Content>
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" 
                                                    BorderThickness="0"
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#106EBE"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                    
                    <!-- Maximize/Restore Button -->
                    <Button x:Name="MaximizeRestoreButton"
                            Click="MaximizeRestoreButton_Click"
                            Width="40" Height="30"
                            Margin="0,0,5,0"
                            Background="Transparent"
                            BorderThickness="0"
                            Cursor="Hand"
                            WindowChrome.IsHitTestVisibleInChrome="True">
                        <Button.Content>
                            <Path x:Name="MaximizeRestorePath"
                                  Stroke="White" 
                                  StrokeThickness="1"
                                  Stretch="None"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center"/>
                        </Button.Content>
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" 
                                                    BorderThickness="0"
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#106EBE"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                    
                    <!-- Close Button -->
                    <Button Click="CloseButton_Click"
                            Width="40" Height="30"
                            Background="Transparent"
                            BorderThickness="0"
                            Cursor="Hand"
                            WindowChrome.IsHitTestVisibleInChrome="True">
                        <Button.Content>
                            <Path Data="M 0,0 L 10,10 M 10,0 L 0,10" 
                                  Stroke="White" 
                                  StrokeThickness="1"
                                  Stretch="None"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center"/>
                        </Button.Content>
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" 
                                                    BorderThickness="0"
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#E81123"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Drawer Menu Panel -->
        <Border x:Name="DrawerPanel"
                Grid.Row="1"
                Width="320"
                HorizontalAlignment="Left"
                Background="{StaticResource SurfaceBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="0,0,1,0"
                Panel.ZIndex="100"
                Visibility="Collapsed">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.3"/>
            </Border.Effect>
            
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="20">
                    <!-- Drawer Header -->
                    <Grid Margin="0,0,0,20">
                        <TextBlock Text="Menu" 
                                   FontSize="20" 
                                   FontWeight="SemiBold" 
                                   Foreground="{StaticResource TextPrimaryBrush}"/>
                        <Button Content="✕" 
                                Click="DrawerMenuButton_Click"
                                Width="30" Height="30"
                                HorizontalAlignment="Right"
                                Style="{StaticResource ModernButtonStyle}"
                                Background="Transparent"
                                Foreground="{StaticResource TextPrimaryBrush}"
                                FontSize="16"/>
                    </Grid>

                    <!-- Search Section -->
                    <Border Background="{StaticResource BackgroundBrush}" 
                            BorderBrush="{StaticResource BorderBrush}" 
                            BorderThickness="1" 
                            CornerRadius="6" 
                            Padding="15" 
                            Margin="0,0,0,15">
                        <StackPanel>
                            <TextBlock Text="{conv:Localize Search}" 
                                       FontSize="14" 
                                       FontWeight="SemiBold" 
                                       Foreground="{StaticResource TextPrimaryBrush}"
                                       Margin="0,0,0,10"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox Grid.Column="0" 
                                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                         Style="{StaticResource ModernTextBoxStyle}"
                                         FontSize="14">
                                    <TextBox.InputBindings>
                                        <KeyBinding Key="Escape" Command="{Binding ClearSearchCommand}"/>
                                    </TextBox.InputBindings>
                                </TextBox>
                                <TextBlock Grid.Column="0" 
                                           Text="{conv:Localize SearchPlaceholder}" 
                                           Margin="10,0,0,0" 
                                           VerticalAlignment="Center"
                                           Foreground="{StaticResource TextSecondaryBrush}"
                                           IsHitTestVisible="False">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding SearchText}" Value="">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                <Button Grid.Column="1" 
                                        Content="✕" 
                                        Command="{Binding ClearSearchCommand}"
                                        Margin="5,0,0,0" 
                                        Width="30"
                                        Style="{StaticResource ModernButtonStyle}"
                                        Visibility="{Binding SearchText, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <!-- Pin Actions Section -->
                    <Border Background="{StaticResource BackgroundBrush}" 
                            BorderBrush="{StaticResource BorderBrush}" 
                            BorderThickness="1" 
                            CornerRadius="6" 
                            Padding="15" 
                            Margin="0,0,0,15">
                        <StackPanel>
                            <TextBlock Text="{conv:Localize PinActions}" 
                                       FontSize="14" 
                                       FontWeight="SemiBold" 
                                       Foreground="{StaticResource TextPrimaryBrush}"
                                       Margin="0,0,0,10"/>
                            <Button Content="{conv:Localize PinText}" 
                                    Command="{Binding PinTextCommand}" 
                                    Style="{StaticResource ModernButtonStyle}" 
                                    Margin="0,0,0,8"
                                    HorizontalAlignment="Stretch"
                                    Padding="0,10"/>
                            <Button Content="{conv:Localize PinImage}" 
                                    Command="{Binding PinImageCommand}"
                                    Style="{StaticResource ModernButtonStyle}" 
                                    Margin="0,0,0,8"
                                    HorizontalAlignment="Stretch"
                                    Padding="0,10"/>
                            <Button Content="{conv:Localize PinFile}" 
                                    Command="{Binding PinFileCommand}"
                                    Style="{StaticResource ModernButtonStyle}"
                                    HorizontalAlignment="Stretch"
                                    Padding="0,10"/>
                        </StackPanel>
                    </Border>

                    <!-- Filters Section -->
                    <Border Background="{StaticResource BackgroundBrush}" 
                            BorderBrush="{StaticResource BorderBrush}" 
                            BorderThickness="1" 
                            CornerRadius="6" 
                            Padding="15" 
                            Margin="0,0,0,15">
                        <StackPanel>
                            <TextBlock Text="{conv:Localize Filters}" 
                                       FontSize="14" 
                                       FontWeight="SemiBold" 
                                       Foreground="{StaticResource TextPrimaryBrush}"
                                       Margin="0,0,0,10"/>
                            
                            <!-- Date Filter -->
                            <StackPanel Margin="0,0,0,10">
                                <TextBlock Text="{conv:Localize FilterByDate}" 
                                           FontSize="12" 
                                           Foreground="{StaticResource TextSecondaryBrush}"
                                           Margin="0,0,0,5"/>
                                <DatePicker SelectedDate="{Binding SelectedDate}" 
                                            FontSize="12" 
                                            Style="{StaticResource ModernDatePickerStyle}"
                                            HorizontalAlignment="Stretch"/>
                            </StackPanel>

                            <!-- Type Filter -->
                            <StackPanel Margin="0,0,0,10">
                                <TextBlock Text="{conv:Localize FilterByType}" 
                                           FontSize="12" 
                                           Foreground="{StaticResource TextSecondaryBrush}"
                                           Margin="0,0,0,5"/>
                                <ComboBox FontSize="12" 
                                          SelectedValue="{Binding SelectedItemType}" 
                                          Style="{StaticResource ModernComboBoxStyle}"
                                          HorizontalAlignment="Stretch">
                                    <ComboBox.Items>
                                        <ComboBoxItem Content="{conv:Localize All}" Tag="{x:Null}" IsSelected="True"/>
                                        <ComboBoxItem Content="{conv:Localize Text}" Tag="0"/>
                                        <ComboBoxItem Content="{conv:Localize Image}" Tag="1"/>
                                        <ComboBoxItem Content="{conv:Localize File}" Tag="2"/>
                                    </ComboBox.Items>
                                    <ComboBox.SelectedValuePath>Tag</ComboBox.SelectedValuePath>
                                </ComboBox>
                            </StackPanel>

                            <!-- Tag Filter -->
                            <StackPanel Margin="0,0,0,10">
                                <TextBlock Text="{conv:Localize FilterByTag}" 
                                           FontSize="12" 
                                           Foreground="{StaticResource TextSecondaryBrush}"
                                           Margin="0,0,0,5"/>
                                <ComboBox FontSize="12" 
                                          SelectedItem="{Binding SelectedTag}" 
                                          ItemsSource="{Binding AllTags}"
                                          Style="{StaticResource ModernComboBoxStyle}"
                                          HorizontalAlignment="Stretch"/>
                            </StackPanel>

                            <Button Content="{conv:Localize ClearFilters}" 
                                    Command="{Binding ClearFiltersCommand}"
                                    Style="{StaticResource ModernButtonStyle}" 
                                    Background="#666666"
                                    FontSize="12" 
                                    Padding="0,8"
                                    HorizontalAlignment="Stretch"/>
                        </StackPanel>
                    </Border>

                    <!-- View Options Section -->
                    <Border Background="{StaticResource BackgroundBrush}" 
                            BorderBrush="{StaticResource BorderBrush}" 
                            BorderThickness="1" 
                            CornerRadius="6" 
                            Padding="15">
                        <StackPanel>
                            <TextBlock Text="{conv:Localize ViewOptions}" 
                                       FontSize="14" 
                                       FontWeight="SemiBold" 
                                       Foreground="{StaticResource TextPrimaryBrush}"
                                       Margin="0,0,0,10"/>
                            <CheckBox IsChecked="{Binding GroupByDate}" 
                                      Content="{conv:Localize GroupByDate}" 
                                      FontSize="12"
                                      Style="{StaticResource ModernCheckBoxStyle}"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <!-- Main Content Area -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <Grid Margin="15">
                <ItemsControl ItemsSource="{Binding GroupedItems}" 
                              Visibility="{Binding GroupByDate, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="0,0,0,20">
                                <!-- Group Header -->
                                <Border Background="{StaticResource BackgroundBrush}" 
                                        Padding="10" Margin="0,0,0,10"
                                        CornerRadius="4">
                                    <TextBlock Text="{Binding DateGroup}" 
                                               FontSize="18" FontWeight="SemiBold"
                                               Foreground="{StaticResource PrimaryBrush}"/>
                                </Border>
                                
                                <!-- Items in Group -->
                                <ItemsControl ItemsSource="{Binding Items}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate DataType="{x:Type vm:PinnedItemViewModel}">
                                            <Border Background="{StaticResource SurfaceBrush}" 
                                                    BorderBrush="{StaticResource BorderBrush}" 
                                                    BorderThickness="1" 
                                                    CornerRadius="6" 
                                                    Padding="15" 
                                                    Margin="0,0,0,10">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Style.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter Property="Background" Value="#F8F9FA"/>
                                                                <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                                                                <Setter Property="BorderThickness" Value="1.5"/>
                                                            </Trigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                                <Border.Effect>
                                                    <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.1"/>
                                                </Border.Effect>
                                                
                                                <Grid>
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition Height="Auto"/>
                                                        <RowDefinition Height="*"/>
                                                        <RowDefinition Height="Auto"/>
                                                    </Grid.RowDefinitions>

                                                    <!-- Header with Date and Type -->
                                                    <Grid Grid.Row="0" Margin="0,0,0,10">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                        </Grid.ColumnDefinitions>
                                                        <StackPanel Grid.Column="0" Orientation="Horizontal">
                                                            <TextBlock Text="{Binding DisplayDate}" 
                                                                       FontSize="12" 
                                                                       Foreground="{StaticResource TextSecondaryBrush}"/>
                                                            <TextBlock Text="{Binding SourceDisplayText}" 
                                                                       FontSize="11" 
                                                                       Foreground="{StaticResource TextSecondaryBrush}"
                                                                       FontStyle="Italic"
                                                                       Margin="10,0,0,0"
                                                                       Visibility="{Binding SourceDisplayText, Converter={StaticResource StringToVisibilityConverter}}"/>
                                                        </StackPanel>
                                                        <TextBlock Grid.Column="2" 
                                                                   Text="{Binding Type}" 
                                                                   FontSize="11" 
                                                                   FontWeight="Medium"
                                                                   Padding="6,2"
                                                                   Background="{StaticResource PrimaryBrush}"
                                                                   Foreground="White"/>
                                                    </Grid>

                                                    <!-- Content based on type -->
                                                    <Grid Grid.Row="1">
                                                        <!-- Text Content -->
                                                        <TextBox Text="{Binding TextContent, Mode=OneWay}" 
                                                                 TextWrapping="Wrap" 
                                                                 MaxHeight="200"
                                                                 FontSize="14"
                                                                 Foreground="{StaticResource TextPrimaryBrush}"
                                                                 IsReadOnly="True"
                                                                 BorderThickness="0"
                                                                 Background="Transparent"
                                                                 Visibility="{Binding Type, Converter={StaticResource ItemTypeToVisibilityConverter}, ConverterParameter=Text}"/>

                                                        <!-- Image Content -->
                                                        <StackPanel Visibility="{Binding Type, Converter={StaticResource ItemTypeToVisibilityConverter}, ConverterParameter=Image}">
                                                            <Image Source="{Binding ImageSource}" 
                                                                   MaxHeight="300" 
                                                                   MaxWidth="600"
                                                                   Stretch="Uniform"
                                                                   HorizontalAlignment="Left"
                                                                   Cursor="Hand">
                                                                <Image.InputBindings>
                                                                    <MouseBinding MouseAction="LeftDoubleClick"
                                                                                  Command="{Binding DataContext.ViewImageCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                                                  CommandParameter="{Binding}"/>
                                                                </Image.InputBindings>
                                                            </Image>
                                                            <TextBlock Text="{Binding ImageMetadata}" 
                                                                       Margin="0,5,0,0"
                                                                       FontSize="11" 
                                                                       Foreground="{StaticResource TextSecondaryBrush}"
                                                                       Visibility="{Binding ImageMetadata, Converter={StaticResource StringToVisibilityConverter}}"/>
                                                        </StackPanel>

                                                        <!-- File Content -->
                                                        <StackPanel Visibility="{Binding Type, Converter={StaticResource ItemTypeToVisibilityConverter}, ConverterParameter=File}">
                                                            <TextBlock Text="{Binding FileName}" 
                                                                       FontSize="14" FontWeight="Medium"
                                                                       Foreground="{StaticResource TextPrimaryBrush}"/>
                                                            <TextBlock Text="{Binding FilePath}" 
                                                                       FontSize="12" 
                                                                       Foreground="{StaticResource TextSecondaryBrush}"
                                                                       Margin="0,5,0,0"/>
                                                            <TextBlock Text="{Binding FileSizeFormatted, Mode=OneWay}"
                                                                       FontSize="11" 
                                                                       Foreground="{StaticResource TextSecondaryBrush}"
                                                                       Margin="0,3,0,0"/>
                                                            <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                                                <CheckBox IsChecked="{Binding IsCached}" 
                                                                          Content="{conv:Localize CacheFileLocally}" 
                                                                          VerticalAlignment="Center"
                                                                          Style="{StaticResource ModernCheckBoxStyle}"
                                                                          Checked="CacheCheckBox_Changed"
                                                                          Unchecked="CacheCheckBox_Changed"/>
                                                                <TextBlock Text="{conv:Localize StoresACopy}" 
                                                                           FontSize="11" 
                                                                           Foreground="{StaticResource TextSecondaryBrush}"
                                                                           Margin="10,0,0,0" 
                                                                           VerticalAlignment="Center"/>
                                                            </StackPanel>
                                                        </StackPanel>
                                                    </Grid>

                                                    <!-- Tags and Actions -->
                                                    <Grid Grid.Row="2" Margin="0,10,0,0">
                                                        <Grid.RowDefinitions>
                                                            <RowDefinition Height="Auto"/>
                                                            <RowDefinition Height="Auto"/>
                                                            <RowDefinition Height="Auto"/>
                                                        </Grid.RowDefinitions>

                                                        <!-- Tags with Add Button -->
                                                        <WrapPanel Grid.Row="0" Margin="0,0,0,5">
                                                            <!-- Existing Tags -->
                                                            <ItemsControl ItemsSource="{Binding Tags}">
                                                                <ItemsControl.ItemsPanel>
                                                                    <ItemsPanelTemplate>
                                                                        <WrapPanel/>
                                                                    </ItemsPanelTemplate>
                                                                </ItemsControl.ItemsPanel>
                                                                <ItemsControl.ItemTemplate>
                                                                    <DataTemplate>
                                                                        <!-- Pinterest-style tag: rounded pill with shadow -->
                                                                        <Border CornerRadius="12" 
                                                                                Padding="10,5" 
                                                                                Margin="0,0,6,6"
                                                                                Background="White"
                                                                                BorderThickness="0">
                                                                            <Border.Effect>
                                                                                <DropShadowEffect BlurRadius="8" 
                                                                                                  ShadowDepth="2" 
                                                                                                  Opacity="0.2" 
                                                                                                  Direction="270"/>
                                                                            </Border.Effect>
                                                                            <StackPanel Orientation="Horizontal">
                                                                                <!-- Tag color indicator -->
                                                                                <Ellipse Width="8" 
                                                                                         Height="8" 
                                                                                         VerticalAlignment="Center"
                                                                                         Margin="0,0,6,0">
                                                                                    <Ellipse.Fill>
                                                                                        <SolidColorBrush Color="{Binding Color, TargetNullValue=#0078D4}"/>
                                                                                    </Ellipse.Fill>
                                                                                </Ellipse>
                                                                                <TextBlock Text="{Binding Name}" 
                                                                                           FontSize="11" 
                                                                                           FontWeight="Medium"
                                                                                           Foreground="#333333"
                                                                                           VerticalAlignment="Center"/>
                                                                                <Button Content="✕"
                                                                                        Click="RemoveTagButton_Click"
                                                                                        Tag="{Binding DataContext, RelativeSource={RelativeSource AncestorType=Border, AncestorLevel=2}}"
                                                                                        CommandParameter="{Binding Name}"
                                                                                        FontSize="10"
                                                                                        Foreground="#999999"
                                                                                        Background="Transparent"
                                                                                        BorderThickness="0"
                                                                                        Margin="6,0,0,0"
                                                                                        Padding="2"
                                                                                        Width="16" Height="16"
                                                                                        Cursor="Hand"
                                                                                        VerticalAlignment="Center">
                                                                                    <Button.Style>
                                                                                        <Style TargetType="Button">
                                                                                            <Setter Property="Template">
                                                                                                <Setter.Value>
                                                                                                    <ControlTemplate TargetType="Button">
                                                                                                        <Border Background="{TemplateBinding Background}" 
                                                                                                                CornerRadius="8"
                                                                                                                Padding="{TemplateBinding Padding}">
                                                                                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                                                        </Border>
                                                                                                    </ControlTemplate>
                                                                                                </Setter.Value>
                                                                                            </Setter>
                                                                                            <Style.Triggers>
                                                                                                <Trigger Property="IsMouseOver" Value="True">
                                                                                                    <Setter Property="Background" Value="#F0F0F0"/>
                                                                                                    <Setter Property="Foreground" Value="#333333"/>
                                                                                                </Trigger>
                                                                                            </Style.Triggers>
                                                                                        </Style>
                                                                                    </Button.Style>
                                                                                </Button>
                                                                            </StackPanel>
                                                                        </Border>
                                                                    </DataTemplate>
                                                                </ItemsControl.ItemTemplate>
                                                            </ItemsControl>
                                                            
                                                            <!-- Add Tag Button -->
                                                            <Button Content="+ Add"
                                                                    Command="{Binding DataContext.ShowAddTagPopupCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                                    CommandParameter="{Binding}"
                                                                    Padding="10,5" 
                                                                    Margin="0,0,6,6"
                                                                    Background="#F5F5F5"
                                                                    BorderBrush="{StaticResource PrimaryBrush}"
                                                                    BorderThickness="1"
                                                                    FontSize="11"
                                                                    FontWeight="Medium"
                                                                    Foreground="{StaticResource PrimaryBrush}"
                                                                    Cursor="Hand"
                                                                    Height="28">
                                                                <Button.Style>
                                                                    <Style TargetType="Button">
                                                                        <Setter Property="Template">
                                                                            <Setter.Value>
                                                                                <ControlTemplate TargetType="Button">
                                                                                    <Border Background="{TemplateBinding Background}" 
                                                                                            BorderBrush="{TemplateBinding BorderBrush}"
                                                                                            BorderThickness="{TemplateBinding BorderThickness}"
                                                                                            CornerRadius="12"
                                                                                            Padding="{TemplateBinding Padding}">
                                                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                                    </Border>
                                                                                </ControlTemplate>
                                                                            </Setter.Value>
                                                                        </Setter>
                                                                        <Style.Triggers>
                                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                                <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
                                                                                <Setter Property="Foreground" Value="White"/>
                                                                            </Trigger>
                                                                        </Style.Triggers>
                                                                    </Style>
                                                                </Button.Style>
                                                            </Button>
                                                        </WrapPanel>

                                                        <!-- Add Tag Section -->
                                                        <!-- Removed: Tag selection moved to preview window -->

                                                        <!-- Actions -->
                                                        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
                                                            <Button Content="{conv:Localize Copy}" 
                                                                    Command="{Binding DataContext.CopyItemCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                                    CommandParameter="{Binding}"
                                                                    Padding="10,5"
                                                                    Margin="0,0,5,0"
                                                                    Background="#F5F5F5"
                                                                    BorderBrush="{StaticResource PrimaryBrush}"
                                                                    BorderThickness="1"
                                                                    FontSize="11"
                                                                    FontWeight="Medium"
                                                                    Foreground="{StaticResource PrimaryBrush}"
                                                                    Cursor="Hand"
                                                                    Height="28">
                                                                <Button.Style>
                                                                    <Style TargetType="Button">
                                                                        <Setter Property="Template">
                                                                            <Setter.Value>
                                                                                <ControlTemplate TargetType="Button">
                                                                                    <Border Background="{TemplateBinding Background}" 
                                                                                            BorderBrush="{TemplateBinding BorderBrush}"
                                                                                            BorderThickness="{TemplateBinding BorderThickness}"
                                                                                            CornerRadius="12"
                                                                                            Padding="{TemplateBinding Padding}">
                                                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                                    </Border>
                                                                                </ControlTemplate>
                                                                            </Setter.Value>
                                                                        </Setter>
                                                                        <Style.Triggers>
                                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                                <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
                                                                                <Setter Property="Foreground" Value="White"/>
                                                                            </Trigger>
                                                                        </Style.Triggers>
                                                                    </Style>
                                                                </Button.Style>
                                                            </Button>
                                                            <Button Content="{conv:Localize Delete}" 
                                                                    Command="{Binding DataContext.DeleteItemCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                                    CommandParameter="{Binding}"
                                                                    Padding="10,5"
                                                                    Background="#F5F5F5"
                                                                    BorderBrush="#D13438"
                                                                    BorderThickness="1"
                                                                    FontSize="11"
                                                                    FontWeight="Medium"
                                                                    Foreground="#D13438"
                                                                    Cursor="Hand"
                                                                    Height="28">
                                                                <Button.Style>
                                                                    <Style TargetType="Button">
                                                                        <Setter Property="Template">
                                                                            <Setter.Value>
                                                                                <ControlTemplate TargetType="Button">
                                                                                    <Border Background="{TemplateBinding Background}" 
                                                                                            BorderBrush="{TemplateBinding BorderBrush}"
                                                                                            BorderThickness="{TemplateBinding BorderThickness}"
                                                                                            CornerRadius="12"
                                                                                            Padding="{TemplateBinding Padding}">
                                                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                                    </Border>
                                                                                </ControlTemplate>
                                                                            </Setter.Value>
                                                                        </Setter>
                                                                        <Style.Triggers>
                                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                                <Setter Property="Background" Value="#D13438"/>
                                                                                <Setter Property="Foreground" Value="White"/>
                                                                            </Trigger>
                                                                        </Style.Triggers>
                                                                    </Style>
                                                                </Button.Style>
                                                            </Button>
                                                        </StackPanel>
                                                    </Grid>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- Ungrouped View (original) -->
            <ItemsControl ItemsSource="{Binding Items}" 
                          Visibility="{Binding GroupByDate, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type vm:PinnedItemViewModel}">
                        <Border Background="{StaticResource SurfaceBrush}" 
                                BorderBrush="{StaticResource BorderBrush}" 
                                BorderThickness="1" 
                                CornerRadius="6" 
                                Padding="15" 
                                Margin="0,0,0,10">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#F8F9FA"/>
                                            <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                                            <Setter Property="BorderThickness" Value="1.5"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.1"/>
                            </Border.Effect>
                            
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <!-- Header with Date and Type -->
                                <Grid Grid.Row="0" Margin="0,0,0,10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                                        <TextBlock Text="{Binding DisplayDate}" 
                                                   FontSize="12" 
                                                   Foreground="{StaticResource TextSecondaryBrush}"/>
                                        <TextBlock Text="{Binding SourceDisplayText}" 
                                                   FontSize="11" 
                                                   Foreground="{StaticResource TextSecondaryBrush}"
                                                   FontStyle="Italic"
                                                   Margin="10,0,0,0"
                                                   Visibility="{Binding SourceDisplayText, Converter={StaticResource StringToVisibilityConverter}}"/>
                                    </StackPanel>
                                    <TextBlock Grid.Column="2" 
                                               Text="{Binding Type}" 
                                               FontSize="11" 
                                               FontWeight="Medium"
                                               Padding="6,2"
                                               Background="{StaticResource PrimaryBrush}"
                                               Foreground="White"/>
                                </Grid>

                                <!-- Content based on type -->
                                <Grid Grid.Row="1">
                                    <!-- Text Content -->
                                    <TextBox Text="{Binding TextContent, Mode=OneWay}" 
                                             TextWrapping="Wrap" 
                                             MaxHeight="200"
                                             FontSize="14"
                                             Foreground="{StaticResource TextPrimaryBrush}"
                                             IsReadOnly="True"
                                             BorderThickness="0"
                                             Background="Transparent"
                                             Visibility="{Binding Type, Converter={StaticResource ItemTypeToVisibilityConverter}, ConverterParameter=Text}"/>

                                    <!-- Image Content -->
                                    <StackPanel Visibility="{Binding Type, Converter={StaticResource ItemTypeToVisibilityConverter}, ConverterParameter=Image}">
                                        <Image Source="{Binding ImageSource}" 
                                               MaxHeight="300" 
                                               MaxWidth="600"
                                               Stretch="Uniform"
                                               HorizontalAlignment="Left"
                                               Cursor="Hand">
                                            <Image.InputBindings>
                                                <MouseBinding MouseAction="LeftDoubleClick"
                                                              Command="{Binding DataContext.ViewImageCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                              CommandParameter="{Binding}"/>
                                            </Image.InputBindings>
                                        </Image>
                                        <TextBlock Text="{Binding ImageMetadata}" 
                                                   Margin="0,5,0,0"
                                                   FontSize="11" 
                                                   Foreground="{StaticResource TextSecondaryBrush}"
                                                   Visibility="{Binding ImageMetadata, Converter={StaticResource StringToVisibilityConverter}}"/>
                                    </StackPanel>

                                    <!-- File Content -->
                                    <StackPanel Visibility="{Binding Type, Converter={StaticResource ItemTypeToVisibilityConverter}, ConverterParameter=File}">
                                        <TextBlock Text="{Binding FileName}" 
                                                   FontSize="14" FontWeight="Medium"
                                                   Foreground="{StaticResource TextPrimaryBrush}"/>
                                        <TextBlock Text="{Binding FilePath}" 
                                                   FontSize="12" 
                                                   Foreground="{StaticResource TextSecondaryBrush}"
                                                   Margin="0,5,0,0"/>
                                        <TextBlock Text="{Binding FileSizeFormatted, Mode=OneWay}"
                                                   FontSize="11" 
                                                   Foreground="{StaticResource TextSecondaryBrush}"
                                                   Margin="0,3,0,0"/>
                                        <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                            <CheckBox IsChecked="{Binding IsCached}" 
                                                      Content="{conv:Localize CacheFileLocally}" 
                                                      VerticalAlignment="Center"
                                                      Style="{StaticResource ModernCheckBoxStyle}"
                                                      Checked="CacheCheckBox_Changed"
                                                      Unchecked="CacheCheckBox_Changed"/>
                                            <TextBlock Text="{conv:Localize StoresACopy}" 
                                                       FontSize="11" 
                                                       Foreground="{StaticResource TextSecondaryBrush}"
                                                       Margin="10,0,0,0" 
                                                       VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </StackPanel>
                                </Grid>

                                <!-- Tags and Actions -->
                                <Grid Grid.Row="2" Margin="0,10,0,0">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <!-- Tags with Add Button -->
                                    <WrapPanel Grid.Row="0" Margin="0,0,0,5">
                                        <!-- Existing Tags -->
                                        <ItemsControl ItemsSource="{Binding Tags}">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <WrapPanel/>
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Border CornerRadius="3" 
                                                            Padding="8,3" 
                                                            Margin="0,0,5,5">
                                                        <Border.Background>
                                                            <SolidColorBrush Color="{Binding Color, TargetNullValue=#0078D4}"/>
                                                        </Border.Background>
                                                        <StackPanel Orientation="Horizontal">
                                                            <TextBlock Text="{Binding Name}" 
                                                                       FontSize="11" 
                                                                       Foreground="White"
                                                                       VerticalAlignment="Center"/>
                                                            <Button Content="✕"
                                                                    Click="RemoveTagButton_Click"
                                                                    Tag="{Binding DataContext, RelativeSource={RelativeSource AncestorType=Border, AncestorLevel=2}}"
                                                                    CommandParameter="{Binding Name}"
                                                                    FontSize="10"
                                                                    Foreground="White"
                                                                    Background="Transparent"
                                                                    BorderThickness="0"
                                                                    Margin="5,0,0,0"
                                                                    Padding="2"
                                                                    Width="16" Height="16"
                                                                    Cursor="Hand"
                                                                    VerticalAlignment="Center">
                                                                <Button.Style>
                                                                    <Style TargetType="Button">
                                                                        <Setter Property="Template">
                                                                            <Setter.Value>
                                                                                <ControlTemplate TargetType="Button">
                                                                                    <Border Background="{TemplateBinding Background}" 
                                                                                            CornerRadius="8"
                                                                                            Padding="{TemplateBinding Padding}">
                                                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                                    </Border>
                                                                                </ControlTemplate>
                                                                            </Setter.Value>
                                                                        </Setter>
                                                                        <Style.Triggers>
                                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                                <Setter Property="Background" Value="#40FFFFFF"/>
                                                                            </Trigger>
                                                                        </Style.Triggers>
                                                                    </Style>
                                                                </Button.Style>
                                                            </Button>
                                                        </StackPanel>
                                                    </Border>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                        
                                        <!-- Add Tag Button -->
                                        <Button Content="+ Add"
                                                Command="{Binding DataContext.ShowAddTagPopupCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                CommandParameter="{Binding}"
                                                Padding="10,5" 
                                                Margin="0,0,6,6"
                                                Background="#F5F5F5"
                                                BorderBrush="{StaticResource PrimaryBrush}"
                                                BorderThickness="1"
                                                FontSize="11"
                                                FontWeight="Medium"
                                                Foreground="{StaticResource PrimaryBrush}"
                                                Cursor="Hand"
                                                Height="28">
                                            <Button.Style>
                                                <Style TargetType="Button">
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="Button">
                                                                <Border Background="{TemplateBinding Background}" 
                                                                        BorderBrush="{TemplateBinding BorderBrush}"
                                                                        BorderThickness="{TemplateBinding BorderThickness}"
                                                                        CornerRadius="12"
                                                                        Padding="{TemplateBinding Padding}">
                                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                </Border>
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                    <Style.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
                                                            <Setter Property="Foreground" Value="White"/>
                                                        </Trigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                    </WrapPanel>

                                    <!-- Actions -->
                                    <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
                                        <Button Content="{conv:Localize Copy}" 
                                                Command="{Binding DataContext.CopyItemCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                CommandParameter="{Binding}"
                                                Padding="10,5"
                                                Margin="0,0,5,0"
                                                Background="#F5F5F5"
                                                BorderBrush="{StaticResource PrimaryBrush}"
                                                BorderThickness="1"
                                                FontSize="11"
                                                FontWeight="Medium"
                                                Foreground="{StaticResource PrimaryBrush}"
                                                Cursor="Hand"
                                                Height="28">
                                            <Button.Style>
                                                <Style TargetType="Button">
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="Button">
                                                                <Border Background="{TemplateBinding Background}" 
                                                                        BorderBrush="{TemplateBinding BorderBrush}"
                                                                        BorderThickness="{TemplateBinding BorderThickness}"
                                                                        CornerRadius="12"
                                                                        Padding="{TemplateBinding Padding}">
                                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                </Border>
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                    <Style.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
                                                            <Setter Property="Foreground" Value="White"/>
                                                        </Trigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                        <Button Content="{conv:Localize Delete}" 
                                                Command="{Binding DataContext.DeleteItemCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                CommandParameter="{Binding}"
                                                Padding="10,5"
                                                Background="#F5F5F5"
                                                BorderBrush="#D13438"
                                                BorderThickness="1"
                                                FontSize="11"
                                                FontWeight="Medium"
                                                Foreground="#D13438"
                                                Cursor="Hand"
                                                Height="28">
                                            <Button.Style>
                                                <Style TargetType="Button">
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="Button">
                                                                <Border Background="{TemplateBinding Background}" 
                                                                        BorderBrush="{TemplateBinding BorderBrush}"
                                                                        BorderThickness="{TemplateBinding BorderThickness}"
                                                                        CornerRadius="12"
                                                                        Padding="{TemplateBinding Padding}">
                                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                </Border>
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                    <Style.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Background" Value="#D13438"/>
                                                            <Setter Property="Foreground" Value="White"/>
                                                        </Trigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                    </StackPanel>
                                </Grid>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            </Grid>
        </ScrollViewer>

        <!-- Status Bar -->
        <Border Grid.Row="2" Background="{StaticResource SurfaceBrush}" 
                BorderBrush="{StaticResource BorderBrush}" 
                BorderThickness="0,1,0,0" 
                Padding="15,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Column="0" 
                           Text="{conv:Localize HotkeyHint}" 
                           Foreground="{StaticResource TextSecondaryBrush}"/>
                
                <TextBlock Grid.Column="1" 
                           Text="{conv:Localize ClipboardMonitoringActive}" 
                           Foreground="{StaticResource PrimaryBrush}"/>
            </Grid>
        </Border>
        
        <!-- Add Tag Popup -->
        <Popup x:Name="AddTagPopup"
               PlacementTarget="{Binding ElementName=MainGrid}"
               Placement="Center"
               AllowsTransparency="True"
               StaysOpen="False">
            <Border Background="{StaticResource SurfaceBrush}"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="20"
                    MinWidth="300">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="15" ShadowDepth="5" Opacity="0.3"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock Text="{conv:Localize AddTag}" 
                               FontSize="16" 
                               FontWeight="SemiBold" 
                               Margin="0,0,0,15"
                               Foreground="{StaticResource TextPrimaryBrush}"/>
                    
                    <TextBlock Text="{conv:Localize TagName}" 
                               FontSize="12" 
                               Margin="0,0,0,5"
                               Foreground="{StaticResource TextPrimaryBrush}"/>
                    <TextBox x:Name="NewTagTextBox"
                             Text="{Binding NewTagName, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource ModernTextBoxStyle}"
                             FontSize="13"
                             Padding="10,8"
                             Margin="0,0,0,15"/>
                    
                    <TextBlock Text="{conv:Localize ExistingTags}" 
                               FontSize="12" 
                               Margin="0,0,0,5"
                               Foreground="{StaticResource TextPrimaryBrush}"/>
                    <ScrollViewer MaxHeight="200" 
                                  VerticalScrollBarVisibility="Auto"
                                  Margin="0,0,0,15">
                        <ItemsControl ItemsSource="{Binding AllTags}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Content="{Binding}"
                                            Command="{Binding DataContext.SelectExistingTagCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                            CommandParameter="{Binding}"
                                            Style="{StaticResource ModernButtonStyle}"
                                            Background="#F5F5F5"
                                            Foreground="{StaticResource TextPrimaryBrush}"
                                            Padding="10,6"
                                            Margin="0,0,0,5"
                                            HorizontalAlignment="Stretch"
                                            HorizontalContentAlignment="Left"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                    
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="{conv:Localize Cancel}"
                                Click="CancelAddTag_Click"
                                Style="{StaticResource ModernButtonStyle}"
                                Background="{StaticResource BorderBrush}"
                                Padding="20,8"
                                FontSize="12"
                                Margin="0,0,10,0"/>
                        <Button Content="{conv:Localize Add}"
                                Command="{Binding AddTagCommand}"
                                Click="AddTagButton_Click"
                                Style="{StaticResource ModernButtonStyle}"
                                Background="{StaticResource PrimaryBrush}"
                                Padding="20,8"
                                FontSize="12"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Popup>
    </Grid>
</Window>
